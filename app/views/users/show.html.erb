<% @tasks = @tasks.order(:priority).all.reverse %>

<%= render 'shared/header', header: "Stuff to Do" %>

<div class='col-md-6 col-md-offset-3'>
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= render 'tasks/task', task: task %>
      </li>
    <% end %>
  </ul>
</div>  
